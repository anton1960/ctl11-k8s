apiVersion: apps/v1
kind: Deployment
metadata:
  name: ctlserver-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      component: server
  template:
    metadata:
      labels:
        component: server
    spec:
      containers:
        - name: server
          image: alu1960/server
          ports:
            - containerPort: 3010